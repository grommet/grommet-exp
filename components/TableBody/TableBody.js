import { __rest } from 'tslib';
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';

const TableBody = forwardRef((_a, ref) => {
    var rest = __rest(_a, []);
    return jsx("tbody", Object.assign({}, rest));
});
TableBody.displayName = "TableBody";

export { TableBody };
